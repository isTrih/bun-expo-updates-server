# Expo Updates Server 环境变量示例
# 请复制此文件为 .env 并填入适当的值

# 服务器设置
port=3000
HOSTNAME=http://localhost:3000
NODE_ENV=production

# OSS 设置 (必填项)
OSS_ACCESS_KEY=你的访问密钥
OSS_SECRET_KEY=你的秘密密钥
OSS_BUCKET=你的桶名称

# OSS 区域设置 (可选，默认为 automatic)
OSS_REGION=automatic

# S3兼容性选项 (可选，用于自定义S3服务)
# OSS_ENDPOINT=https://your-custom-s3-endpoint.com
# OSS_FORCE_PATH_STYLE=0

# 日志设置 (可选)
# LOG_LEVEL=info
